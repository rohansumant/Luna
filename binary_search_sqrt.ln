@sqrt(val) {
    l = 0;
    r = val;
    while(l < r) {
        m = (l+r+1)/2;
        if((m*m) > val) {
            r = m-1;
        };
        if((m*m) <= val) {
            l = m;
        };
    };
    return(l);
}

@main() {
    print(sqrt(100));
    return(0);
}

main()
